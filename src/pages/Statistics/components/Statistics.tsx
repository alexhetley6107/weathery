import React from 'react';
import s from './Statistics.module.scss';

interface Props {}

const Statistics = (props: Props) => {
	return <div className={s.home}>Statistics</div>;
};

export default Statistics;
